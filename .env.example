# Environment Configuration
# Set NODE_ENV to one of: dev, staging, production, local
NODE_ENV=dev

# Debug Settings
DEBUG_NETWORK=false
DEBUG_CONSOLE=false

# Override specific settings if needed
# PLAYWRIGHT_BASE_URL=https://custom-url.com
# PLAYWRIGHT_TIMEOUT=30000
# PLAYWRIGHT_RETRIES=2
# PLAYWRIGHT_HEADLESS=true